{
  "name": "browser-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/sidepanel.js --bundle --format=esm --target=chrome120 --outfile=extension/sidepanel.js",
    "watch": "npm run build -- --watch",
    "agent": "node server/agent-server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.278",
    "@anthropic-ai/sdk": "^0.127.0",
    "dompurify": "^3.4.15",
    "marked": "^18.0.13",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
